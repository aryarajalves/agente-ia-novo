# Dependências (backend/requirements.txt)

Toda nova biblioteca adicionada ao backend deve:

1. Ter versão fixa (`==`).
2. Ter um comentário na linha acima explicando para que serve no projeto.

Depois de adicionar, rodar `docker compose build api` (a partir de `docker/`) para validar que a imagem builda.
